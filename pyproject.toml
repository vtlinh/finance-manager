[project]
name = "finance-manager"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic",
    "cryptography",
    "flask",
    "google-auth-oauthlib",
    "gspread",
    "monarchmoney",
    "python-dotenv",
]

[project.scripts]
finance-run = "manager.main:main_sync"
finance-server = "server:run"

[tool.hatch.build.targets.wheel]
packages = ["manager"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
