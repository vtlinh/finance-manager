[project]
name = "finance-manager"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic",
    "flask",
    "google-auth-oauthlib",
    "gspread",
    "monarchmoney",
    "python-dotenv",
]
